{
  "name": "medilink",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -n api,web \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "build": "concurrently -n api,web \"npm --prefix apps/api run build\" \"npm --prefix apps/web run build\"",
    "lint": "concurrently -n api,web \"npm --prefix apps/api run lint\" \"npm --prefix apps/web run lint\"",
    "typecheck": "concurrently -n api,web \"npm --prefix apps/api run typecheck\" \"npm --prefix apps/web run typecheck\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
